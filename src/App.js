import '@/App.css';
import tmdbLogo from '@/assets/tmdb_logo.svg';

const App = () => {
  return (
    <div className="App">
      <header className="App-header">
        Nothing here! Where could it be?
        <img src={tmdbLogo} />
      </header>
    </div>
  );
};

export default App;
